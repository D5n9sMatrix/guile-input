module genious.express;

import std.algorithm;
import std.array;
import std.bigint;
import std.digest;
import std.range;


void either(either, fill, appender)(refRange obj)
{
    struct only
    {
        either obj;    // area resolut values
        fill obj;      // aremsy python 
        appender obj;  // bool stic

        padLeft obj;   // pp py
        padRight obj;  // bit side
    }
retro obj;
}        
