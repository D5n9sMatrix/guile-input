#!/usr/bin/r

# eigth running level measure
e1 <- complex(length.out = 0L, real = numeric(), imaginary = numeric(), modulus = 1, argument = 0)
# fill appenders linear obj
f1 <- append(e1, 100, after = length(p1))
# appenders obj
ap1 <- s1
# pad left fants oranges
pad1 <- as.numeric(e1, f1)

