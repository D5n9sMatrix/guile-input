#!/usr/bin/r

import pycode
from cars as start


class express:
    # python ignore=errors, ignore=args
    def pycode(self, IDE=True, program=True, console=True, feature=True):
        self.__doc__ = IDE;
        self.__doc__ = program;
        self.__doc__ = console;
        self.__doc__ = feature;

        if IDE is True:
            if program is True:
                print(IDE)
                pass
            else:
                print(program)
                pass
        else:
            print(IDE)
            pass
