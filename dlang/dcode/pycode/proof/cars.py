#!/usr/bin/python3

class proof:
    def cars(self, popup=True):
        if popup is True:
            try:
                OsError("*args: object")
            except Exception:
                print("*args: cassava")
                pass
        else:
            return popup
            pass
                        
