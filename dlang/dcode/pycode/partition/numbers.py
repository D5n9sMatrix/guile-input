#!/usr/bin/python3

class partition:
    def numbers(self, num=None, part=-0, prog=1, state=2):
        if num is None:
            if part == -0:
                print(num)
            else:
                return num
                pass
        else:
           try:
                OsError("*args: object")
           except Exception:
            print(num)
        raise num
        pass
              
        
