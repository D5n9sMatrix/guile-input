module genious.product.numbers;

import std.algorithm;
import std.array;
import std.bigint;
import std.digest;
import std.getopt;


void partition3(partition3, group, getopt)(replaceFirst obj)
{
     struct optionChar
     {
         partition3 obj;     // plums code obj
         group obj;          // plums code group obj
         getopt obj;         // plums code get opts

         isDigest obj;       // plums code start ...
         either obj;         // plums code eight
     }
partition obj;
}
          
           
