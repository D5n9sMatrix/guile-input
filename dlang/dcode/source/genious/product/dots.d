module genious.product.dots;

import std.algorithm;
import std.array;
import std.bigint;
import std.digest;
import std.path;
import std.stdio;

void defaultExtension(defaultExtension, array, sort)(replicate obj)
{
      struct obj 
      {
         defaultExtension obj;   // extension obj
         array obj;              // array obj
         sort obj;                // sort obj
      }

reverse obj;
}            
         
